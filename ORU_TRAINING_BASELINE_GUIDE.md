# ORU 变阻抗装配训练指南

> 当前 ORU 任务架构：策略输出 12D 变阻抗参数 (Kp + Kd)，目标位姿固定，域随机化在初始状态。
> 奖励函数：单点指数位置奖励 `exp(-20·pos_error)`。

---

## 1. 整体架构

```
                        一个 step (15Hz)

  观测 (43D)
     │
     ▼
  ┌──────────┐    ┌───────────┐
  │  LSTM    │───▶│   MLP     │───▶ 动作 a_t (12D)
  │ 2×1024   │    │ 512→128→64│    [Kp_scale(6), Kd_scale(6)]
  └──────────┘    └───────────┘         │
                                        │ EMA 平滑 (0.2)
                                        ▼
                                 ┌─────────────┐
                                 │ Kp = base×scale │
                                 │ Kd = base×scale │
                                 └──────┬──────┘
                                        │
                                        ▼
                                 ┌─────────────┐
 固定目标                          │ 阻抗控制器    │ 8次 @ 120Hz
 [gnd_x,gnd_y,0.436,0,0,1,0] ──▶│ Task PD +    │────────────▶
                                 │ Nullspace    │  关节力矩 τ
                                 └─────────────┘
```

---

## 2. 策略输出：12D 变阻抗参数

### 动作空间

```
a_t = [Kp_x, Kp_y, Kp_z, Kp_rx, Kp_ry, Kp_rz,    Kd_x, Kd_y, Kd_z, Kd_rx, Kd_ry, Kd_rz]
       └────────── Kp 缩放 (6D) ──────────┘       └────────── Kd 缩放 (6D) ──────────┘
       范围: [-1, 1]                               范围: [-1, 1]
```

### 从策略输出到阻抗增益

```python
# oru_env.py _apply_action
scale_kp = 1.0 + actions[:, 0:6] * gain_range     # gain_range = 2.0
scale_kp = clamp(scale_kp, 0.05, 5.0)
task_prop_gains = base_gains * scale_kp

scale_kd = 1.0 + actions[:, 6:12] * gain_range
scale_kd = clamp(scale_kd, 0.05, 5.0)
task_deriv_gains = base_deriv * scale_kd
```

### 基础增益与范围

| 方向 | base_Kp | base_Kd | Kp 范围 | Kd 范围 |
|------|---------|---------|---------|---------|
| XYZ (平移) | **100** | **20** | 5 ~ 300 | 1 ~ 60 |
| RxRyRz (旋转) | **100** | **20** | 5 ~ 300 | 1 ~ 60 |

> Kp=100 对齐 Factory 任务水平。Kd=20 = 2√100 为临界阻尼。

---

## 3. 目标位姿

**固定不变**，策略只控制阻抗，不控制"往哪走"：

```python
ctrl_target_ee_pos = [ground_x, ground_y, 0.436]  # XY 跟 ground，Z 固定 0.436
ctrl_target_ee_quat = [0, 0, 1, 0]               # wxyz, 180° Y 轴旋转
```

目标 Z=0.436 来源：从 "对接完成位置正上方 30cm 处" 的关节角反向标定得到。

---

## 4. 阻抗控制器

### 数学模型

```
τ = Jᵀ · F_task  +  N · τ_null

F_task = Kp · (x_desired − x_current) + Kd · (0 − ẋ)
N = I − Jᵀ · (J·M⁻¹·Jᵀ)⁻¹ · J·M⁻¹
τ_null = N · M · (kp_null·(q_default−q) + kd_null·(−q̇))
```

### 关键参数 (当前最新值)

| 参数 | 值 | 说明 |
|------|----|------|
| `Kp` | 策略控制 (5~300) | 6D 可变比例刚度 |
| `Kd` | 策略控制 (1~60) | 6D 可变微分阻尼 |
| 关节底层阻尼 | **15.0 N·m·s/rad** | 每个关节独立粘滞阻尼 |
| 关节速度限制 | **10 rad/s** | 允许关节快速运动 |
| `kp_null` | 1.0 | 零空间比例增益 |
| `kd_null` | 2.0 | 零空间临界阻尼 |
| `dead_zone` | `[0.1,0.1,0.1, 0.05,0.05,0.05]` | ＜0.1N/0.05Nm 力归零 |

### 振荡修复历程

| 修复点 | 最终值 | 效果 |
|--------|--------|------|
| XY 权重 | 移除 `xy_weight=2.0` → 1.0 | 停止 XY 方向过度振摆 |
| 零空间阻尼 | `kd_null` 0.1 → 2.0 | 关节内部振荡大幅减少 |
| 死区 | 添加 `[0.1, ..., 0.05]` | 消除目标附近的极限环抖动 |
| 关节阻尼 | 添加 `damping=15.0` | **关键修复** — 覆盖零空间和任务空间外的振荡 |
| ORU kinematic | `True` → `False` | 解决接触地面后"卡死不动" |

---

## 5. 奖励函数

### 当前奖励（v2 — 单点指数衰减）

```python
pos_error = ||ee_pos − target_pos||₂
rew = exp(−20 × pos_error)                         # 主奖励 [0→1]
    − 0.01 × ||actions||₂                           # 动作幅度惩罚
    − 0.001 × ||Δactions||₂                         # 动作变化惩罚
    + success.float()                               # 判定成功 +1
```

> 最终 reward 是所有 16 个并行环境每步平均值在 225 步 (15s) 上的累加。

### 奖励函数演进

| 版本 | 公式 | 问题 |
|------|------|------|
| v0 (三阶段关键点) | `squashing(kp_dist, a=5/50/100, b=4/2/0)` | 关键点随姿态旋转，方向不一致导致信号错乱 |
| v1 (单点 1/(1+x)) | `1/(1+pos_error)` | 梯度太平，5cm→30cm 奖励差异仅 0.18 |
| **v2 (单点 exp)** | **`exp(−20·pos_error)`** | 梯度充足，当前使用 |

### 奖励随位置误差变化

| 位置误差 | `exp(−20x)` | 说明 |
|----------|-------------|------|
| 30 cm | 0.002 | 起始位置，基本无奖励 |
| 10 cm | 0.14 | |
| 5 cm | 0.37 | |
| 2 cm | 0.67 | |
| 1 cm | 0.82 | 当前收敛位置 (~159 total) |
| 5 mm | 0.90 | 成功判定的 XY 阈值 |
| 1 mm | 0.98 | 接近满分 |
| 0 mm | 1.00 | 理论最大值 |

---

## 6. 奖励上限与收敛分析

### 理论最大奖励

| 组件 | 每步最大值 | 225 步总计 |
|------|-----------|-----------|
| 位置奖励 `exp(−20x)` | 1.0 (误差=0) | **225** |
| 动作惩罚 | 0 | 0 |
| 成功奖励 | 1.0 (每步判定成功) | **+225** |
| **总计** | | **450** |

但 `pos_error=0` 和"每步判定成功"实际上不可能同时达到（ORU 接触地面后存在残差力），实际天花板约 220。

### 为什么收敛在 ~160？

训练达到 reward≈159 时平均位置误差约 **1.7cm**。继续训升不上去的根因分析：

| 因素 | 影响 |
|------|------|
| **目标 Z 不准** | 目标 Z=0.436 是从关节角推算的，ORU 实际接触地面的 EE 高度 ≈0.395。控制器推不到 0.436 就被地面卡住 → 残差 ~4cm → 奖励上限锁死 |
| **自适应学习率** | PPO `lr_schedule: adaptive` 随 KL 散度下降自动降 lr，收敛后期 lr 可能降至 1e-5 以下 → 参数几乎不动 |
| **域随机化 ±12cm** | 每个 episode 起始位置不同，有些偏移大的场景很难收敛到 1cm 以内 → 拉低平均值 |
| **成功阈值过严** | XY<5mm 对于 1.7cm 平均误差来说太紧 → 成功判定始终为 0 |
| **单点奖励无方向** | `exp(−20x)` 只关心距离，不区分 XYZ 方向 → 策略不知道"Z 方向最重要" |

### 改进方向

1. **重新标定目标 Z**：用 play_force 实测 ORU 接触地面时的 EE Z 值，把目标 Z 设到实测值
2. **分方向加权奖励**：Z 方向误差权重 > XY 方向 (装配时 Z 精准最重要)
3. **两阶段奖励**（后续创新点）：初段鼓励 XY 对齐 + 姿态对齐；接触后鼓励力平滑 + Z 方向推进
4. **Curriculum 域随机化**：先训小偏移 (0~4cm)，逐步放大到 12cm
5. **宽松成功 → 收紧**：先定义较宽的成功条件让策略拿到 positive sparse reward，再逐步收紧

---

## 7. 观测空间

### Policy 观测 (43D)

| 分量 | 维度 | 说明 |
|------|------|------|
| `ee_pos_rel_ground` | 3 | EE 相对 ground 的位置 |
| `ee_quat` | 4 | EE 相对 ground 的姿态 (ground_q⁻¹ × ee_q) |
| `ee_linvel` | 3 | EE 线速度 (有限差分) |
| `ee_angvel` | 3 | EE 角速度 (有限差分) |
| `joint_pos` | 6 | 6 个关节角度 |
| `task_prop_gains` | 6 | 当前 Kp（策略看到自己上一步的刚度） |
| `task_deriv_gains` | 6 | 当前 Kd（策略看到自己上一步的阻尼） |
| `prev_actions` | 12 | 上一帧 12D 动作 |

### Critic 状态 (56D)

Policy 全部 + `ground_pos(3)` + `ground_quat(4)` + `pos_threshold(3)` + `rot_threshold(3)`

---

## 8. 域随机化

### 默认初始关节角

UR5 从对接完成时的关节角启动，EE 在目标位置正上方 ~30cm 处：

```python
joint_pos = {
    "shoulder_pan_joint":  -15.60°,
    "shoulder_lift_joint": -82.61°,
    "elbow_joint":          29.85°,
    "wrist_1_joint":       -37.15°,
    "wrist_2_joint":       -89.96°,
    "wrist_3_joint":        74.46°,
}
```

### IK 初始位姿随机化（reset 时）

```
1. 设置 UR5 到默认关节角 → FK 得到 home EE pose（目标正上方 ~30cm）
2. 加入随机偏移: 位置 ±12cm (XYZ)，姿态 ±3° (各轴)
3. DLS IK → 随机初始关节角
4. 写入 UR5 初始状态
```

```python
ik_rand_pos_noise = (0.12, 0.12, 0.12)     # ±12cm
ik_rand_rot_noise = (0.052, 0.052, 0.052)  # ±3° (0.052 rad)
```

### 目标不随机化

目标位姿始终是 `[ground_x, ground_y, 0.436, 0,0,1,0]`。策略从不同起点学会到达同一个目标。

---

## 9. 训练配置

### PPO 参数

| 参数 | 值 | 说明 |
|------|----|------|
| `num_envs` | 16 | 场景数量 |
| `num_actors` | 128 | (被 train 脚本覆写为 16) |
| `episode_length_s` | 15 | 225 步策略动作/episode |
| `decimation` | 8 | 策略 15Hz，物理 120Hz |
| `ema_factor` | 0.2 | 动作平滑 |
| `gamma` | 0.995 | 长期规划 |
| `lr` | 1e-4 | 自适应 (KL 阈值 0.008) |
| `entropy_coef` | 0.0 | 无熵正则 |
| `max_epochs` | 200 / 500 | 取决于续训配置 |

### 网络架构

```
LSTM (2层, 1024) → MLP (512→128→64) → 12D 输出
```

### 场景设置

```
clone_in_fabric=False   ← FixedJoint 链条必须在每个 env 独立创建
num_envs=16
env_spacing=2.0
```

---

## 10. 成功判定

```python
xy_dist = ‖target_xy − ee_xy‖₂
z_diff  = ee_z − target_z
success = (xy_dist < 5mm) AND (z_diff < −2.5mm)   # 当前阈值
```

| 参数 | 值 | 说明 |
|------|----|------|
| `xy_tolerance` | 0.005 | 5mm 中心对准 |
| `success_threshold` | 0.05 | Z 低于目标 5%×0.05=2.5mm |

> 当前训练中 success_rate 始终为 0，因为平均误差 ~1.7cm 离 5mm 还有差距。

---

## 11. 与 force1 场景对比

| 参数 | force1 | ORU (RL) |
|------|--------|----------|
| 关节阻尼 | 15.0 | 15.0 |
| 速度限制 | 10 | 10 |
| ORU kinematic | False | False |
| 死区 | `[0.1, ..., 0.05]` | `[0.1, ..., 0.05]` |
| 初始关节角 | 相同 | 相同 |
| 目标 Z | 0.436 | 0.436 |
| Kp | 手写 `[400,400,200,200,200,200]` | 策略变阻抗 `[5~300]` |
| Kd | 手写 `[0,0,0,0,0,0]` | 策略变阻尼 `[1~60]` |

---

## 12. 物理仿真参数

| 参数 | 值 | 说明 |
|------|----|------|
| `dt` | 1/120 | 8.33ms |
| `max_position_iteration_count` | 192 | 防穿透 |
| `solver_type` | 1 | TGS |
| `gpu_max_num_partitions` | 1 | 单 GPU 分区 |
| `static_friction` | 1.0 | RL 场景 |
| `dynamic_friction` | 1.0 | RL 场景 |

---

*最后更新: 2026-07-23*
